<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="Theme.LaunchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             Flutter draws its first frame -->
        <item name="android:defaultFocusHighlightEnabled">false</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:windowTranslucentStatus">true</item>
</style>
    <!--Used by MainActivity-->
    <style name="Theme.NormalTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:defaultFocusHighlightEnabled">false</item>
        <item name="android:fitsSystemWindows">false</item>
    </style>


    <!--Used by FirstActivity-->
    <style name="FirstActivityTheme" parent="Theme.Material3.Light">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <!--Used by SecondActivity-->
    <style name="SecondActivityTheme" parent="Theme.AppCompat.DayNight">
        <item name="android:defaultFocusHighlightEnabled">false</item>
        <item name="android:fitsSystemWindows">false</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:colorBackground">@color/colorPrimary</item>
    </style>
</resources>